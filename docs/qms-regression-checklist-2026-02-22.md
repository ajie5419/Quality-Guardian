# QMS 前端回归清单（2026-02-22）

## 自动化检查结果

- `pnpm lint`：通过
- `pnpm check:type`：通过
- `pnpm check:qms-arch`：通过
- `pnpm exec vitest run apps/web-antd/src/views/qms/supplier/composables/useSupplierActions.test.ts apps/web-antd/src/views/qms/inspection/issues/composables/useIssueActions.test.ts --dom`：通过（4 tests）
- `pnpm exec vitest run apps/web-antd/src/views/qms/workspace/composables/useWorkspaceTaskDispatch.test.ts --dom`：通过（3 tests）

## 9 模块回归矩阵

说明：

- `自动通过`：已有自动化检查覆盖并通过（编译/规则/单测）
- `需人工回归`：涉及页面交互与业务流，需在浏览器手工验证

| 模块 | 列表分页+刷新 | 新增/编辑提交 | 导入/导出一致性 | 结论 |
| --- | --- | --- | --- | --- |
| 工单管理 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 需人工回归 | 需人工回归 |
| 质量策划 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 需人工回归 | 需人工回归 |
| 检验管理 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 需人工回归 |
| 售后质量 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 需人工回归 |
| 供应商管理 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 需人工回归 |
| 外协管理 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 需人工回归 |
| 质量知识库 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 无导入导出主流程 | 需人工回归 |
| 车辆调试 | 自动通过 + 需人工回归 | 自动通过 + 需人工回归 | 需人工回归（日报导出） | 需人工回归 |
| 报表分析 | 自动通过 + 需人工回归 | 不适用 | 需人工回归（导出） | 需人工回归 |

## 人工回归执行命令（本地）

```bash
pnpm dev:antd
```

## 人工回归最小路径（建议）

1. 工单管理：按工单号筛选 -> 翻页 -> 编辑保存 -> 导出。
2. 质量策划（DFMEA/BOM/ITP）：切换项目 -> 新增条目 -> 保存 -> 刷新验证。
3. 检验管理：问题列表筛选 -> 新建问题 -> 导出（含图片列）验证。
4. 售后质量：工单号筛选 -> 编辑 -> 导出（含图片）验证。
5. 供应商管理/外协管理：筛选 -> 编辑供应商 -> 导出验证。
6. 质量知识库：分类切换 -> 新建知识 -> 编辑保存 -> 删除验证。
7. 车辆调试：问题台账新增/关闭 -> 生成日报 -> 预览/导出。
8. 报表分析：日报与汇总页切换周期 -> 导出验证。

## 导出专项回归（本轮新增）

1. 导出上限保护：当筛选结果超过 `20000` 条时，前端应提示  
   `导出数据量超过上限（20000 条），请缩小筛选范围后重试`。
2. 导出过程提示：点击导出后应出现“正在导出，请稍候...”，完成后提示“导出成功”。
3. 导出失败提示：服务异常时应出现明确失败文案，不允许“无响应”。
4. 全量导出准确性：导出行数应等于当前筛选结果总数（在上限内），不应退化为“当前页”。
5. 重点模块验证：

- 工单管理
- 供应商管理
- 外协管理
- 质量损失
- 检验记录
- 质量策划-项目资料

## 本轮结论

- 自动化门禁：全部通过。
- 高风险点（交互和导出）：已列入人工回归路径与导出专项回归，建议完成后再发布。
