NODE_ENV=production
PORT=3000

# Local-only test database. Do not point this at production.
DATABASE_URL=mysql://qms:qms123456@host.container.internal:3307/quality_guard_local_test

REDIS_URL=redis://host.container.internal:6380
REDIS_ENABLED=true
REDIS_OPTIONAL=true

UPLOAD_DIR=/app/uploads

# Keep imported production snapshots unchanged during local startup.
CONTAINER_DEV_RUN_MAINTENANCE=false
