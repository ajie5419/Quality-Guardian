import type { RouteRecordRaw } from 'vue-router';

// System routes are now dynamically loaded from backend via /api/menu/all
// This file is kept empty to avoid duplicate menu entries
const routes: RouteRecordRaw[] = [];

export default routes;
