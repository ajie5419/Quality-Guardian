---
description: Start a new planning session using the planning-with-files skill
---

1.  Use `view_file` to read `.agent/skills/planning-with-files/SKILL.md` to understand the workflow.
2.  Initialize the "Three-File Pattern" (`task_plan.md`, `progress.md`, `findings.md`) in the current workspace, following the templates described in the skill.
3.  Ask the user for the initial goal or task to populate `task_plan.md`.
